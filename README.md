# Haptica Homebrew Tap

```bash
brew tap hapticasensorics/tap
brew install hapticasensorics/tap/1context
```

The formula is named `onecontext` internally because Homebrew formula classes
cannot start with a digit. The tap provides `Aliases/1context`, so the public
install command remains `1context`.
